define = window?define or require(\amdefine) module
require, exports, module <- define

@ # this is a STUB module
# XXX/TODO figure out how to tell requirejs to ignore requires on the client (sometimes we require something only on the server side, but share the code client-side)
