#!/bin/sh
sudo NODE_ENV=$NODE_ENV bin/launch-elasticsearch && \
bin/launch-redis && \
sudo NODE_ENV=$NODE_ENV bin/launch-haproxy && \
sudo NODE_ENV=$NODE_ENV bin/launch-varnish && \
bin/launch-indexer && \
bin/launch-search-notifier
