#!/bin/bash -e

# main
# ---------
if [ "$NODE_ENV" = production ] ; then
  node_modules/LiveScript/bin/livescript app/main.ls 2>> error_log
else
  export NODE_WORKERS=${NODE_WORKERS:=1}
  node_modules/LiveScript/bin/livescript app/main.ls
fi
