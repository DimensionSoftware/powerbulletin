body > div
  display none

body > .fancybox-wrap.fancybox-opened
  display block

// would be nice to infer this stuff from sales-url-mappings
body.super, body.superSites, body.superUsers
  .SuperAdmin-root
    display block

body.homepage > .Sales-root
  display block
// end nice to infer stuff

// vim:fdm=marker
