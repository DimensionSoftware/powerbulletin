
mixin forum(f, i)
  h3.title Forums
    //-a.mutant(href=f.uri)= f.title
  if f.forums
    ul.forum(class="#{f.classes || ''} forum-#{activeForumId}")
      each sf,ii in f.forums
        mixin subforum(f, sf, ii)

mixin subforum(f, sf, i)
  //-.subforum(class="#{(i % 2) ? 'odd' : 'even'}")
  li
    a.mutant.title(href=sf.uri)= sf.title

mixin thread(t)
  li.thread
    h4.title
      a.mutant(href=t.uri)= t.title
    .user-name= t.user_name
    .post-count= t.post_count
    .created= t.created_human

//- main
//- ---------
//-if forums
//-  each f,i in forums
//-    mixin forum(f, i)

//-if threads
//-  each t,i in threads
//-    mixin thread(t, i)

//-h3.title Threads
#handle.handle(title='Toggle') &lt;
.create
  a(class='button onclick-add-post-dialog') Create Thread

ul.threads
  each t,ii in topThreads
    mixin thread(t)

//- vim:fdm=marker
