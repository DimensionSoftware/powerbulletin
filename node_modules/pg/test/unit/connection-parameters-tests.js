var assert = require('assert');

var ConnectionParameters = function(config) {
  
};

//parses a connectionString into a hash of
//connection parameters
ConnectionParameters.parse = function(connectionString) {
  
};

//uses defaults
var param = new ConnectionParameters();
assert.equal(param.host, 
